---
title: opencode-await
description: Simple, blocking command execution for AI agents - no polling loops needed
template: splash
hero:
  tagline: Stop your AI agent from spinning in polling loops. Run long commands that block until complete, then return concise success/failure output.
  actions:
    - text: Get Started
      link: /getting-started/installation/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/rothnic/opencode-await
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<div class="hero-visual">
  <div class="comparison">
    <div class="before">
      <span class="label">Without opencode-await</span>
      <div class="flow">
        <span class="step">Run command</span>
        <span class="arrow">→</span>
        <span class="step loop">Poll status</span>
        <span class="arrow">→</span>
        <span class="step loop">Poll again</span>
        <span class="arrow">→</span>
        <span class="step loop">Poll again...</span>
        <span class="arrow">→</span>
        <span class="step">Parse output</span>
      </div>
      <span class="cost">5+ API calls per command</span>
    </div>
    <div class="after">
      <span class="label">With opencode-await</span>
      <div class="flow">
        <span class="step accent">await_command</span>
        <span class="arrow">→</span>
        <span class="step success">Done</span>
      </div>
      <span class="cost">1 API call, blocks until complete</span>
    </div>
  </div>
</div>

<div class="quick-install-container">

## Quick Install

```json
// opencode.json
{
  "plugin": ["opencode-await"]
}
```

</div>

## Features

<CardGrid>
  <Card title="Blocking Execution" icon="rocket">
    Agent session pauses until command completes. No polling loops, no wasted API calls.
  </Card>
  <Card title="Concise Output" icon="document">
    Returns clean success/failure status instead of raw terminal dumps.
  </Card>
  <Card title="Simple Tool Calls" icon="pencil">
    One intuitive function call replaces complex shell command construction.
  </Card>
  <Card title="AI Summarization" icon="star">
    Optionally summarize verbose output into actionable insights.
  </Card>
</CardGrid>
